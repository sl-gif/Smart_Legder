package com.example.mytestapplication.data.datasource.database

import android.util.Log
import androidx.room.RoomDatabase
import androidx.sqlite.db.SupportSQLiteDatabase
/*

class MyDatabaseCallback : RoomDatabase.Callback() {
    override fun onCreate(db: SupportSQLiteDatabase) {
        super.onCreate(db)
        Log.d("MyDatabaseCallback", "Database created")

        // Add your insertion log statement here
        db.execSQL("INSERT INTO my_table (column1, column2) VALUES ('value1', 'value')")
        Log.d("MyDatabaseCallback", "Insertion occurred")
    }

    override fun onOpen(db: SupportSQLiteDatabase) {
        super.onOpen(db)
        Log.d("MyDatabaseCallback", "Database opened")
    }


}
*/
